# Persistence reliability V2 — execution ledger

Authority: Founder explicitly approved V2 in this conversation on 2026-09-24.
Environment: existing local Work and worktree; principal orchestration and three Luna/high executors/reviewers. No cloud execution.

## Baseline

- Local HEAD at approval: `59a042c`; origin/main after fetch: `fe1eb15a49ee5baac855d49f5580335ac5f07ae1`.
- Production /health: same main SHA, version `d18858f2-380d-45e6-aa24-a8ff8681a324`.
- Control Tower deployments/version read through Wrangler: 100% version `fac275aa-7a0c-4209-a3b5-2315d59b97fb`, tag `git-cb9352645e63fd448727bd4771d01c47642ef2da`, created 2026-09-21T16:20:30.452Z. Guard and Tower therefore have distinct deployed revisions. GitHub PR-run lookup for Guard main SHA returned no rows (not proof of failed or absent CI).
- Read-only GitHub REST follow-up resolved baseline CI: main `fe1eb15a` has successful CI run `35771710714` and successful deployment run `35771766910`.
- Fresh context PRJ-0003 failed at Control Tower deadline: correlation `4d6ca3b2-0ecf-49dc-9292-4cfe6926dad2`.
- Fresh SOP request-status failed at the same boundary: correlation `7f3d57fa-f2ab-4e9c-b0ca-bc134983077a`.
- Fresh Dropbox reads: PRJ-0003 snapshot and materialization head 354/PV5; active/requested null and no pending obligations. PRJ-0007 head and human views 112. PRJ-0002 human views 170. These observations do not establish transport readiness.
- SOP request `TXN-PRJ0003-RESEARCHSOP-20260924T064940Z-Q7M4`: receipt committed 352→353, execution freshly read finalized/terminal true, certificate `finalizations/3eb59fb1446fdd5af9c914bf4c399508625798bd4bb6d891445c944de4568d6a.json` freshly read and bound to request hash `b46648af5447282b170fddf8b461e62a344d7992fac3cc97a73c21cda96e63d2`, revision 353/PV5. No replay needed or issued.
- Partial Registry/fallback changes belong to E6; two red MCP transport tests belong to E5. Preserve unrelated `worker-configuration.d.ts`.
- No ancestor AGENTS.md found in the specified repository ancestry; chat project instructions remain applicable.

| Lot | Owner | State | Next action / evidence |
|---|---|---|---|
| E0 | Principal | reviewed | Both deployed versions observed; canonical baseline and SOP certificate read; 39 baseline tests pass; baseline CI/deploy verified through REST |
| E1 | Principal | reviewed | b443ba7 adds explicit verified wake evidence after B review; 7 observation tests, 17 with fallback rerun green; owner adapters being integrated |
| E2 | A | reviewed | 726073f; canonical cache-loss path, bounded observations and strict receipt bindings reviewed by C; principal final 39 tests green after kind allowlist/artifact intent consistency fixes |
| E3 | A | reviewed | 4dbe2db; principal 31 tests pass: monotone proof, shared 5-second/32-call budget, newer checkpoint resume, no repeated checkpoint write |
| E4 | B | reviewed | 2c7691a + fa0a23d + 5ef0f54; principal 8 context tests/typecheck pass; escaped text and project/revision binding regressions fixed |
| E5 | B | reviewed | 7bc0214; C reviewed MCP and operator deadline correction; principal 37 targeted tests and typecheck passed before subsequent lots |
| E6 | C | reviewed | de02d12 + fa0a23d + b443ba7 + e1a6ce6; principal 15 tests green, canonical cache loss/allocation/lost response and key rotation retain identity |
| E7 | A | reviewed | f0e229b + eb31bf5: provider Retry-After and Dropbox timeout recovery; durable scoped legacy range cursor; PV6 coalescence gaps blocked visibly; no-progress finalization backoff/stop6 with intent preserved. Independent C review non-blocking; principal 59 E7/workload/cadence/sequence targeted tests and typecheck passed |
| E8 | C | reviewed | 37d1183 quarantines proven missing file targets; 534ab4e includes actual capacity and covered-obligation checks. Principal reran 127 E8/E9 tests; provider failures remain retryable, no old cursor starvation |
| E9 | B | reviewed | 534ab4e and 564f2bc: bounded canonical reconstruction, read-only diagnostics, four-view proof and 1000 eligible-reference paged scan. Warm-head provider drift now prevents acknowledgement; principal reran 64 E9 tests; stale scan checkpoint superseded by exact externally published head without head rewrite |
| E10 | C | reviewed | 05f059e + 7ee4a1f: fresh strict admissions/domain refusals/payload collisions on four entries, frozen approval evidence survives lost acknowledgments then finalizes; real inbox capacity refusal preserves the exact request without artificial receipt; principal tests green and fault boundaries mapped |
| E11 | Principal | verified | Scoped capabilities and diagnostic fixes deployed on Guard/Tower 2cbd797; native, connected-app and real classic calls verified. Server leaves client mounting unknown rather than falsely claiming absence. |
| E12 | Principal + reviewers | qualified | Latest PR #254 head `1e92044` passed CI `36121356214`; principal 257 files/1,652 tests, types/static/search-off and dry-run evidence recorded below. Independent code and fixture reviews accepted. |
| E13 | Principal | verified | Guard/Tower exact main `2cbd797`, official deployments `36123025879` / `36123920327` successful. PRJ-0003 357/PV6 and PRJ-0007 121/PV6 aligned; no pending obligations. Known statuses finalized, including formerly busy PRJ-0007. No replay. Historical earlier deployment findings remain below for traceability. |
| E14 | Principal + C | blocked_external | Four real governed document probes committed/finalized/read back; thirty planned cross-surface fresh reads and four formerly busy status probes succeed. 39 handler completions, maximum 3,602 ms. Standalone local Codex tasks still output-empty despite prior diagnostics; Codex-agent success does not qualify that distinct client. |
| E15 | Principal + C | blocked_external | Canonical report including full SOP matrix committed/finalized and exact body read back: `DOCREQ-PRJ0002-PERSISTENCE-E15-20260925T103412Z`, hash `b9064870…`. Full acceptance remains unsigned due standalone-client gate; broad SOP tasks not falsely completed. |

Post-deploy read-only follow-up, 17:40Z: PRJ-0007 Dropbox-synced state/head/views all rev118/PV6, convergence active/requested null and pending obligations zero. PRJ-0003 remains rev357/PV5 published with PV6 pending and no blocked error. Two further Control Tower context reads (PRJ-0003 and PRJ-0007) both exceeded the 10-second handler deadline; E13 cannot be accepted on a single earlier warm success. A separate local TDD change now distinguishes the two known Guard 503 status codes at Control Tower without leaking dependency payloads; this change is not yet committed, merged, or deployed and cannot be counted as a production fix. No request was replayed.

No global completion claim until all required gates have evidence. Unknown production observations remain unknown.

First PR-head CI `36015661958` on `370b45f` failed in a finalization-slice test assertion: a shared mock and a denied 33rd `beforeHttp` attempt were counted as provider calls. The counter has been scoped to the runtime and permitted calls; 38 targeted tests, independent review and a sixth complete local suite (257 files/1,631 tests) pass. CI on the revised head remains mandatory. No merge or deploy occurred on the failed SHA.

Second PR-head CI `36017834529` on `f0cb640` failed two further prototype-wide spy assertions in `execution-guard.spec.ts` while persisted cursor and stopped-failure checks passed. The fixture now binds those observations to the specific project/candidate and Durable Object instance. Targeted 38 tests pass; independent review, full suite and new CI pending. No merge or deploy occurred on this failed SHA.

Follow-up fixture audit found other prototype/mock-wide counts in the same file. They now use exact candidate IDs and durable queues, or a journal counter bound to project/request. The targeted file stays 38/38 green. A third PR-head CI must qualify the final SHA; previous failed runs do not authorize production.

Third PR-head CI `36021122090` on `5c22a90` failed a provider-conflict fixture: the prototype-wide one-shot `readMaterializationHead` rejection was consumed before the intended project's finalization, leaving the real absent head to produce `materialization_head_mismatch`. The runtime correctly classified that HTTP 409; no production defect is demonstrated by this failure. The temporary-provider, Retry-After, network-timeout, access and conflict fixtures now inject the failure only for their own project and delegate all other reads. Independent review concurred with the fixture diagnosis. Targeted 38/38, typecheck, and a fresh full local suite (257 files/1,631 tests) pass; PR-head CI remains required. No merge or deploy occurred on the third failed SHA.

Fourth PR-head CI `36023368241` on `7eba9d7` failed six tests after 11m15s of the full suite: three heavy integration cases timed out (two at 5 seconds, one at 20); a finalization fixture's 5 ms real-clock deadline expired before its first candidate read; one qualification cost test used generic `fetch` instead of the provider-only calls it intended to count; and the 50-vs-1,000-commit provider cost test observed 32 vs 33 calls without an endpoint trace. The latter remains a real test failure of unknown origin, not a production defect disproven by local success. The fixture clock is now deterministic, only the three heavy test timeouts are expanded, provider-only qualification calls are counted, and both cost tests now expose endpoint/path histograms on failure. Six targeted files/107 tests, typecheck and a fresh full local suite (257 files/1,631 tests) pass. A new CI run must establish the cause of any remaining count difference. No merge or deploy occurred on the fourth failed SHA.

Fifth PR-head CI `36026523970` on `1f12844` reached the full suite and had one remaining assertion: the qualification fixture's shared ProjectGuard stub observed five calls rather than four while the canonical qualification proof is explicitly limited to four distinct project states. The strict count was contaminated by scheduled project work, not evidence of a per-file REVIEW scan. The fixture now requires four-to-eight guard calls, exact four audit project states, zero REVIEW file-metadata calls, at most 50 provider calls for 160 REVIEW files, and no more than eight additional provider calls versus four files; this rejects linear per-file I/O while tolerating bounded shared alarm traffic. The separate 50-vs-1,000-commit cost equality passed in this CI. Independent review found no blocker in the revised I/O gate. Targeted 53/53, typecheck, and a fresh complete local suite (257 files/1,631 tests) pass. No merge or deploy occurred on the fifth failed SHA.

E12 preparation: `8173021` rejects JSON-RPC/MCP errors carried by HTTP 200 in deployment qualification. Principal six tests and typecheck passed; C independently reviewed helper and callers. An unauthenticated public probe remains explicitly insufficient for the real-client gate.

Read-only transport recheck, before current changes deploy: both the native `mcp__project_os_control_tower` handle and connected-app `mcp__codex_apps` handle returned the SOP request's committed receipt and finalized/terminal execution on 2026-09-24. This confirms that specific historical lookup at observation time, not a new client submission or stable transport guarantee. No transaction was replayed.

Git coordination: an agent amended the shared HEAD while another agent had just committed. Read-only reflog/tree comparison showed no lost content: `fa0a23d` replaces `f2822c4` and includes the E4 escaped-text fix plus a small E6 observation correction. No reset/rebase was performed. Principal now exclusively stages and commits all reviewed changes; agents edit only their owned files.

E13 production follow-up, 17:46–18:08Z: PRJ-0003 Dropbox-synced state, materialization head 357/PV6, and all four current views agree at revision 357; progress has no active/requested target or pending obligation. The exact revision-357 transaction `TXN-PRJ0003-SMWARM-R0-FULL-20260924T114500Z` returns a committed receipt and finalized request status from Control Tower. PRJ-0007 advanced independently to canonical 119 while its previous head was 118/PV6; that new work is tracked separately. No business request was replayed.

2026-09-25 E13/E14 follow-up: PR #251 exact head `d8f79d2475a126d11b122858eaef05e760df3801` passed CI `36043272380` (257 files/1,640 tests, check and dry-runs), merged to main `f8c26952cd37f2abfc4a3463009b13e1d278dbe5`. Guard deployment `36045573146` and Control Tower deployment `36046683655` succeeded on that main SHA; the latter needed a rerun of a cost-profile test contaminated by a scheduled alarm from another fixture. Guard `/health` and Tower capabilities report the exact SHA. Thirty fresh Work-local context reads across PRJ-0003/0007 succeeded without error, client maximum 5,630 ms and p95 5,010 ms; these are not handler-only measurements. PRJ-0003 state/head/four views all rev357/PV6, no active/requested target or pending obligation. PRJ-0007 independently reached rev121/PV6 with state/head/four views aligned, no pending work, and the known rev121 execution finalized with certificate. Neither project received a new business request from this qualification.

E14 new classic chat `6ab57b89-f714-83ea-a3da-ffd2d5efbac5`: capabilities, PRJ-0002 context rev170, known PRJ-0003 receipt 356→357 and finalized status actually callable. Governed document `DOCREQ-PRJ0002-PERSISTENCE-CLASSICNEW-20260924T194137Z` initially returned unknown and status busy, then the same conversation read its committed receipt and finalized/terminal execution; the physical `WORKING/QUALIFICATION-PERSISTENCE-20260924/classic-new.md` was read back. No replay. Work-local request `DOCREQ-PRJ0002-PERSISTENCE-WORKLOCAL-20260925T071454Z`, digest `57e9e844303ff19fed86a0447f87b0ac4dc96eb925202f0d0df0e0a60a83cc04`, similarly returned unknown at the initial call; its bound receipt later showed committed, its execution finalized/terminal with certificate, and `WORKING/QUALIFICATION-PERSISTENCE-20260924/work-local.md` was physically read back. The Work-local plugin subsequently returned both receipt and finalized status. Initial unknown/busy remains a client-experience defect to assess, not proof of lost persistence or authority to resubmit.

An archived, inactive PRJ-0002 classic chat `6a99b747-54f0-83ea-8284-810039a8cfa8` was then selected without touching any active Founder chat. It discovered and actually called capabilities, fresh PRJ-0002 context rev170, and the known Work-local receipt/finalized status. Its one distinct governed write `DOCREQ-PRJ0002-PERSISTENCE-CLASSICOLD-20260925T072442Z`, digest `e43e33d0c6b9a3e590d12eb6e815a224dd080ba628be8c748285102575385460`, initially returned unknown and two read attempts were unavailable/busy. Later read-only lookup in the same old chat returned its committed receipt and finalized/terminal execution; the physical `WORKING/QUALIFICATION-PERSISTENCE-20260924/classic-existing.md` was read back. The chat was re-archived. No request replay.

The separate Codex qualification task `01a0d4f7-0ce5-7e10-a871-516b6232f144` was created locally without a worktree; multiple follow-up turns completed with no visible items or tool evidence, including one explicitly routed to Luna/high. A same-directory local fork `01a0d77e-c473-70b0-97ad-13a9ba3ddfeb` also completed a read-only qualification turn with empty items, then returned no answer to a one-word diagnostic. Neither standalone task's gate is credited; this remains a distinct `blocked_external` client-runtime condition.

A separate Luna/high Codex agent `/root/e14_review` actually called native Control Tower capabilities, fresh PRJ-0002 context rev170, and the known Work-local committed/finalized receipt and status. It then submitted exactly one distinct governed document `DOCREQ-PRJ0002-PERSISTENCE-CODEX-20260925T075410Z`, body digest `82bceec3a939529e5400ad5ea240ebace52cf09a7e84d9b7ae3c9046cd7ff94b`. The initial POST returned unknown; receipt lookup later showed committed and status returned transient READ_BUSY before reaching finalized/terminal with a certificate. Principal read back the physical `WORKING/QUALIFICATION-PERSISTENCE-20260924/codex-agent.md` and verified the body digest after generated frontmatter. There was no second submission. This proves the Codex-agent surface, not the silent standalone-task runtime. E15 still has no completion certificate.

The first CI for PR #251 (`36036411462`, `6fe1ae1`) failed one unrelated search-rebuild assertion in its first attempt; the isolated test passed locally and the failed-job rerun completed successfully on the exact same SHA. A bounded read series nevertheless reproduced 10-second context timeouts. Correlated Guard/Control Tower live traces show requests `2480f023-775d-4a6b-89f8-79449d5911bf` (PRJ-0003) and `833b70f0-567f-45b3-bbb1-ad29f072b745` (PRJ-0007) entered Guard but completed 200 only after 19.361s and 37.101s, respectively, beyond Control Tower's 10-second limit. Source review found that `SearchSyncProjectGuard.serializeSearch` queued observational GETs behind POST finalization despite the diagnostic layer's bypass. Local TDD reproduces this for four read routes. An independent review caught a false-absence race; the fix returns identity-bound busy/unknown while outer mutation work waits, yet still serves locally or canonically proven committed receipts and rejects wrong-project queries. The final local suite passed 257 files/1,640 tests, TypeScript and Guard dry-run, and independent review found no blocker. At that observation point the fix was local only and the timeouts were unresolved; subsequent PR #251 deployment and 30-read production result are recorded above.

2026-09-25 capability-honesty follow-up: production SHA `f8c26952...` still returned `missing_client_capability: PROJECT_OS_CONNECTOR_UNAVAILABLE` from the Codex-agent capabilities call even while that same agent successfully called the connector and committed/finalized a governed document. This is a demonstrated false manifest claim, separate from the earlier search queue fix (which is deployed and passed the 30-read Work-local series). TDD first reproduced the manifest error, then removed the unconditional field while preserving `callable_in_this_session:null`; both MCP and public HTTP capability tests cover the contract. Local suite 257 files/1,640 tests, typecheck, static gates and two dry-runs passed. PR #252 had a three-file diff after merging `origin/main` into the existing branch without a force-push; exact head `b2712bd4609a079b9d4605e51e2fb0059d66d128` passed CI `36108887946`, then was squash-merged to main `0039c88f11c6cfde757a48e4bd177131e69ff9cf`. Official Guard deploy run `36110400049` succeeded; public `/health` reports that exact SHA and Worker version `5c77a92c-da86-4883-9efd-103b55ff1494`. Official Control Tower deploy run `36111138842` was launched after Guard and remains in progress at this ledger entry; no Tower production claim for the new SHA yet.

Control Tower deploy run `36111138842` later failed in its pre-deployment `Verify project` step; it did **not** publish the Worker. The precise failure was `test/execution-guard.spec.ts:1209`, expecting the persisted `[9201]` finalization candidate but observing an absent work cursor. The fixture had advanced fake `Date` 60,001 ms after leaving an alarm at +60,000 ms, allowing a due alarm to race its direct test invocation. The test-only correction moves the preinstalled alarm to +60,000 ms from the advanced clock, disables that direct call's *initial* wake (not the budget-exhaustion continuation), and asserts that the exhausted slice moves the alarm earlier while keeping candidate 9201. Targeted 38/38, typecheck, and a fresh complete local 257-file/1,640-test suite pass; independent review found no runtime change or blocker. PR #253 net diff is one test file, head `2af45e3bd013a0435bc5081ef241cc8101295965`; CI `36112923852` is in progress. No Control Tower deployment is claimed before this PR merges and a new exact-main workflow succeeds.

PR #253 CI `36112923852` passed on its exact head and was squash-merged to main `b3d77771ac7b240c5c8f1cb22f6d6d648f05c33c`. Official Guard deployment `36114573012` was dispatched on that exact SHA; its result and the subsequent exact-SHA Control Tower deployment remain to be verified. The failed prior Tower run remains a pre-publication failure, not production evidence.

Guard deployment `36114573012` subsequently completed successfully, including its exact identity/health verification and the full 257-file/1,640-test prepublication suite. A separate public `/health` read returned SHA `b3d77771ac7b240c5c8f1cb22f6d6d648f05c33c` and Worker version `65f29a27-23cd-454c-bd62-7a5708493e4c`. Control Tower deployment `36115378232` was dispatched on the same main SHA and remains to be verified. Neither the Tower publication nor E14/E15 acceptance is inferred from the Guard result.

Control Tower deployment `36115378232` then completed successfully, including the full 257-file/1,640-test prepublication suite and OAuth-boundary verification. Read-only Wrangler deployment listing shows 100% Tower version `98ee850b-f96f-4aec-8e48-bf5aecaaded9`, tagged `git-b3d77771ac7b240c5c8f1cb22f6d6d648f05c33c`. Both native and connected-app capability calls return that exact deployment SHA and no unconditional `missing_client_capability` field; they correctly leave `callable_in_this_session:null` because the server cannot know client mounting. Fresh native context reads returned PRJ-0003 revision 357 and PRJ-0007 revision 121. The known PRJ-0003 revision-357 status returned `finalized/terminal:true`; the known PRJ-0007 revision-121 receipt returned `committed`, but a fresh status call returned transient `PROJECT_OS_READ_BUSY`, so that status remains a separate reception check. No transaction was replayed.

Post-deploy reception found a remaining status-read defect: PRJ-0007's exact revision-121 execution has Dropbox-synced `progress.json` showing `finalized/terminal:true` and certificate `98923b213957dd5db16490c87db31bb91b60589825a9cefa54558ad0feabf8d8.json`, bound to its request hash and commit. Repeated live request-status reads still return READ_BUSY while fresh context succeeds. Independent source review identified both the search queue and base mutation queue returning busy before probing an already-finalized status. Which queue was occupied in the live call is not established because both use the same code. The minimal TDD correction is assigned to Luna/high: allow a bounded, identity-checked finalized observation through either busy boundary; retain unknown/busy for absent, incomplete, invalid or unavailable evidence. This changes no mutation, receipt or recovery behavior. E15 is not signed while the live status defect remains.

The new integration regression reproduced RED for both occupied boundaries: real ExecutionJournal admission/progress, committed receipt and finalization certificate in DropboxMock still yielded HTTP 503 instead of finalized HTTP 200. The absent-request controls stayed green with unknown/READ_BUSY. No production mutation was used to reproduce the defect.

Independent review required reading the certificate itself rather than accepting its reference. Further RED cases reproduced false success with a missing/altered certificate. The corrected busy path now validates certificate content/hash and exact family receipt bindings using the existing five-second/32-provider-call runtime, then returns immediately without reading recovery payloads. Idle reads retain their prior behavior. Transaction, managed-document and artifact fixtures pass; absent/malformed/misbound/unavailable proof stays unknown, with zero uploads during status reads. Luna/high targeted 42/42, TypeScript and diff check pass; principal static gates, search-sync-off 4/4 and both Worker dry-runs pass. The complete suite and integration remain pending at this entry. The helper supports the existing family-specific finalization certificates; unsupported legacy/generic certificate shapes remain unknown under contention.

Principal complete suite passed 257 files/1,652 tests. Code committed as `8a6002b`, then origin/main `b3d7777` merged into the existing branch with no source/test tree change; resulting PR head `e716f133ffba2c7f87689b97d14c6fe0f7aec641`. A push transport disconnect was resolved by read-only `ls-remote`, which confirmed that exact remote head; no force-push was used. PR #254 has only the three reviewed files. CI `36119693780` is running; no new deployment is claimed until its gates and exact-main publication succeed.

CI `36119693780` subsequently failed in `test/execution-guard.spec.ts:1153`: the deadline fixture expected HTTP 202 and observed 200. The high-risk targeted gate passed; the complete-suite failure still blocks merge. The job-log connector exceeded its response transport limit, so the exact assertion was read from GitHub's job annotations in the integrated browser. Luna/high owns investigation of this fixture; the principal independently checked that production finalization and alarms use the serialization queue, whereas the fixture invokes a private method directly and advances Date across pending alarms. This is a hypothesis to verify, not yet a claimed correction. Production remains unchanged.

The fixture race was reproduced deterministically: an explicitly due alarm consumed candidate 9101 under the advanced clock, then the unsynchronized direct invocation drained 9102–9105 and returned 200. The temporary injection was removed. The test-only fix removes the residual alarm and invokes the deadline slice under the production serialization boundary with no initial wake; assertions still demand HTTP 202, the exact four remaining candidates and a newly armed alarm. Independent Luna/high review accepted this scope; targeted repetitions, TypeScript and diff checks passed. Commit `1e92044f324f2ee842b285b6b06c3879b664ac4c` was pushed to PR #254. CI `36121356214` and the principal's full local suite are in progress. No production code was altered for this fixture correction.

Principal full suite on `1e92044` completed successfully: 257 files / 1,652 tests, 185.73 seconds, exit 0. The server CI has passed its targeted persistence gate and is running its complete suite. This does not yet authorize a claim of deployment.

CI `36121356214` completed successfully, including complete tests, global checks and dry-run. PR #254 merged with an expected-head guard on `1e92044f324f2ee842b285b6b06c3879b664ac4c`, producing main `2cbd7979f4d45307fbae92f4f81a32dc42c5914c`. Official Guard deployment `36123025879` was dispatched against this exact SHA. Pre-publication inspection also confirmed existing Control Tower handler timing is available through filtered Worker logs: a PRJ-0007 context read on the previous version took 1,037 ms, correlation `ae304d36-b755-4d3d-8b46-4fb79d21fa4b`. This single observation is not the final cross-surface series.

Both official deployments succeeded: Guard `36123025879`, version `8196974f-5394-45b6-aa0b-41497574181e`; Tower `36123920327`, version `bff00ccb-153d-4266-895f-9cfe634ca8c3`. Both serve exact main 2cbd797, confirmed by health, deployment listing and native/connected capability probes. The previously busy PRJ-0007 request is now finalized through all four working surfaces without replay. Thirty planned cross-surface context reads succeeded; filtered handler diagnostics (including ancillary probes) recorded 39 completions, max 3,602 ms/p95 2,936 ms. One separate plugin transport error remains documented. The old qualification chat was re-archived. Standalone local Codex tasks remain an external, unproven client gate; the agent route does not substitute for them.

E15 submission frozen before first send: `DOCREQ-PRJ0002-PERSISTENCE-E15-20260925T103412Z`, operation `working.write`, path `QUALIFICATION-PERSISTENCE-20260925/e15-reception.md`, created_at `2026-09-25T10:34:12Z`, content SHA-256 `b906487071131cc5c9ff7f1438d52d4a96306fb952c08912241b1bf4b7f1994f`. Body is the reception report plus complete refreshed SOP matrix, separated by a Markdown rule. Fresh PRJ-0002 context was revision 170. Server obtains admission context; no business transaction or phase closure is being submitted. Receipt and finalization must be verified before claiming persistence.

E15 first POST returned `unknown / PROJECT_OS_SUBMISSION_UNAVAILABLE`, correlation `eefef26d-56c9-4474-94d6-ce1ca8213b73`. No resubmission occurred. Lookup of the original ID subsequently returned committed receipt for `DOC-19676955508B294D171A84B4`, version `VER-REQ-61156610EA22E64D739EE751`, provider revision `0165c4c44928bcd000000037a835733`. At 10:35:35Z status returned finalized/terminal, correlation `2126ff18-1e09-4e92-b20e-d7bcc13cc91b`, certificate `a31498e457fee75abd179cac58782632d9f7ff6640f48acaa5987ea42a83fa73.json` under execution `694a814eb8e70971e049a4bd1382e44bfcb7da4ca090fd330bdb44c7503ebec0`. Dropbox-synced physical file contains the server's project/document frontmatter followed by the exact 17,024-character submitted body; its body SHA-256 equals `b906487071131cc5c9ff7f1438d52d4a96306fb952c08912241b1bf4b7f1994f`. This is persisted technical reception evidence, not full E15 acceptance or completion of the broad SOP tasks. No canonical source file was edited directly.

Independent C verified the original receipt, finalized certificate and byte-exact physical body. Its only wording finding was that the prepared report still described its own receipt as pending. A narrow version-bound wording update is frozen as `DOCREQ-PRJ0002-PERSISTENCE-E15-STATUS-20260925T103911Z`, expected version `VER-REQ-61156610EA22E64D739EE751`, same logical path, content SHA-256 `e44fea187647b6ec06c6aef60b24fc281eaaeb76bf0775b0a1fb9a44cb921b07`. It explicitly refers to the verified original publication and preserves the standalone-client/SOP reservations; no business task or phase is changed. The original body remains in evidence commit `7c4f211` and governed history.

The wording update also completed without resubmission after an initial unknown response: receipt committed, version `VER-REQ-03E3B96D94463D33880BE57C`, provider revision `0165c4c57082317000000037a835733`; status finalized/terminal at 10:40:40Z, correlation `503d00b0-6262-456e-80dd-1fe864957828`, certificate `7c8d358a663db368790ab90ce449ac848afac840b95ebcaeb689aee403a6470b.json`. Physical body readback matches current local report+matrix byte-for-byte, hash `e44fea187647b6ec06c6aef60b24fc281eaaeb76bf0775b0a1fb9a44cb921b07`. The final report no longer presents its initial publication as pending. Full-plan acceptance still requires the separately unproven standalone client; no agent or local process is left running by this handoff.

# Persistence reliability V2 — reception status (2026-09-25)

This is a technical evidence report, not a new business decision or a declaration that the global SOP programme is complete. Its canonical Project OS document receipt and physical readback are pending.

## Verified

- PR #251 merged to main `f8c26952cd37f2abfc4a3463009b13e1d278dbe5`; Guard and Control Tower deployed that SHA. Thirty fresh Work-local context reads across PRJ-0003/0007 returned successfully, with 5,630 ms maximum **client** duration. This is not handler-only timing.
- PRJ-0003 canonical state, materialization head and four current views aligned at revision 357/PV6; no active/requested target or pending obligation at observation. Its known revision-357 transaction was committed and finalized. PRJ-0007 independently aligned at revision 121/PV6 and its known revision-121 execution finalized with a certificate. Later project work must be checked against a fresh head, not these historical revisions.
- Four distinct governed PRJ-0002 qualification documents were submitted from an existing classic ChatGPT chat, a new classic chat, Work local and a separate Luna/high Codex agent. Each has a committed receipt, finalized/terminal execution, certificate and physical content readback. Request IDs and hashes are in the acceptance matrix.
- All four submissions first returned an unknown result or temporarily busy status, but later lookup of the **same** request ID recovered the original receipt. None was resubmitted.
- PR #252 passed CI (257 files/1,640 tests plus type/static/dry-run gates) and was merged to main `0039c88f11c6cfde757a48e4bd177131e69ff9cf`. Its three-file fix removes the unconditional false `missing_client_capability` claim. A subsequent Tower deployment was stopped by an alarm-race test fixture before publication; PR #253 corrected only that fixture, passed CI, and merged to main `b3d77771ac7b240c5c8f1cb22f6d6d648f05c33c`. Official Guard and Tower deployments `36114573012` / `36115378232` succeeded. Guard `/health` serves this exact SHA as Worker version `65f29a27-23cd-454c-bd62-7a5708493e4c`; Tower deployment listing shows 100% version `98ee850b-f96f-4aec-8e48-bf5aecaaded9` tagged with the same SHA. Both native and connected-app capability probes report the exact SHA without the false client-absence field.
- Post-deployment native fresh contexts returned PRJ-0003 revision 357 and PRJ-0007 revision 121. Both projects' Dropbox-synced state, materialization head (PV6), and four current views match their respective revisions; all 324/324 and 365/365 obligations are verified, with `requested` and `active` null. The known PRJ-0003 revision-357 execution was `finalized/terminal:true`. The PRJ-0007 revision-121 receipt was `committed`; its Dropbox-synced execution progress and bound finalization certificate say `finalized/terminal:true`, but the fresh Control Tower status route returned `PROJECT_OS_READ_BUSY`. The physical proof does not make that read route reliable.

The four distinct PRJ-0002 qualification receipts are `DOCREQ-PRJ0002-PERSISTENCE-CLASSICNEW-20260924T194137Z`, `DOCREQ-PRJ0002-PERSISTENCE-CLASSICOLD-20260925T072442Z`, `DOCREQ-PRJ0002-PERSISTENCE-WORKLOCAL-20260925T071454Z` and `DOCREQ-PRJ0002-PERSISTENCE-CODEX-20260925T075410Z`. Their finalized files are in `WORKING/QUALIFICATION-PERSISTENCE-20260924/` as `classic-new.md`, `classic-existing.md`, `work-local.md` and `codex-agent.md` respectively. These are real governed reports, not invented business decisions.

## Post-PR254 reception

PR #254 merged as main `2cbd7979f4d45307fbae92f4f81a32dc42c5914c` after CI `36121356214` succeeded. Principal full suite: 257 files / 1,652 tests. The fix makes finalized status readable across both busy boundaries only after validating the actual bound finalization certificate; absent, incomplete or invalid proof stays unknown. Two independent review findings were corrected before integration. A later CI fixture race was reproduced and isolated without weakening production code or cursor/alarm assertions.

Guard deployment `36123025879` succeeded; public health identifies Worker `8196974f-5394-45b6-aa0b-41497574181e` on the exact main SHA. At 10:24:45Z, the previously busy PRJ-0007 request returned `finalized`, `terminal:true`, its original revision-121 committed receipt and certificate `98923b213957dd5db16490c87db31bb91b60589825a9cefa54558ad0feabf8d8.json`, correlation `bec9d810-770a-46e1-954f-46ec36b6ce39`. No transaction was replayed. Tower deployment `36123920327` also succeeded; deployment listing confirms 100% Worker `bff00ccb-153d-4266-895f-9cfe634ca8c3` tagged with the same exact SHA. Both native and connected-app capability probes report 2cbd797. PRJ-0007 status was rechecked after both deployments at 10:33:20Z: finalized/terminal, correlation `1ddc2882-e85c-4fb5-a186-bb9efab629f0`.

A read-only cross-check around 10:17Z found PRJ-0003 at 357 and PRJ-0007 at 121, four view identities/revisions matching each respective head, all 324/324 and 365/365 obligations verified, and no requested/active work. These checks did not recalculate all view content hashes.

After Guard publication, thirty planned fresh context calls were performed across the two classic qualification chats (ten each), Work local (five) and the Codex agent (five), all returning revisions 357/121 without regression. Each surface also observed the existing PRJ-0007 request as finalized: correlations `6af9a865-5fed-428c-99f8-83536c2697d6` (new classic), `02632759-9857-438e-b854-105b4807cd31` (old classic), `be206b9e-4320-49c1-aff4-56ecdaf0b1c8` (Work), and `3f004627-922d-43f1-add3-c5b712fd8499` (agent). The old chat was re-archived afterward. No active Founder chat was touched.

Filtered existing Control Tower diagnostics captured 39 completed handlers (35 context reads including ancillary probes; four request-status reads), maximum 3,602 ms and p95 2,936 ms. Sanitized correlation-level observations are retained in `2026-09-25-persistence-handler-observations.json`; raw request headers and credentials are excluded. These are handler times, not model/UI latency. The observed projects were quiescent; no artificial production load was created. A separate connected-plugin transport error occurred before the final five Work reads and remains an observed transient, not a claim of zero platform errors. This series ran with Guard 2cbd797 and unchanged Tower code at b3d777 before Tower alignment.

## Not yet accepted

- Two separately created standalone local Codex tasks completed turns with empty outputs, even for a one-word diagnostic. The Codex **agent** route is proven, but the standalone-task client runtime is an external dependency and is not inferred to work.
- The original Work-only timing gap is now covered by the cross-surface handler observations above, but standalone Codex task qualification remains separate from the working Codex-agent route. No synthetic load or future uptime guarantee is inferred.
- Initial unknown/READ_BUSY remains a user-visible delay even though recovery was lossless. It is not represented as a business rejection or a guaranteed fast response.
- Global SOP G01–G14 remain partial at full accepted scope. The rule-governance engine is deployed, but a Dropbox-synced revision-5 snapshot shows only one active global destination rule; a fresh RegistryGuard head and full source-to-control inventory are still required. See the SOP coverage refresh.
- PRJ-0003 historical archives, PRJ-0007 folder/index cleanup, PRJ-0008 closure and package/binary archive gaps are not silently repaired by this runtime delivery.

## SOP programme boundary

The accepted global programme L0–L10 remains open. A read-only Dropbox-synced governance snapshot at revision 5 contains one active global rule (`RULE-GLOBAL-REVIEW-DESTINATION` v2); it is not a fresh RegistryGuard head. The protected live governance route correctly refused an unauthenticated read, and the ordinary Control Tower client does not expose that authority. PRJ-0002 has existing pending tasks `TASK-RECTIFYBASE001`, `TASK-GOVRUNTIME001` and `TASK-RECTIFYPROOF001`; none is marked complete by this runtime report.

All fourteen accepted guarantees G01–G14 are currently **partial at their full scope**, not absent and not proven end-to-end. The remaining work is: accepted-source-to-check inventory (L0); fresh global/local rule and activation proof (L1–L2); refusal parity across every governed entry (L3); postcondition evidence for each operation family (L4); document/package/phase controls and approved archival disposition (L5); observed external drift invalidation (L6); independent review (L7); proof binding to deployed code (L8); typed historical repairs for PRJ-0002/0003/0007/0008 (L9); and final global qualification (L10). No rule is activated from its prose alone, no archive is moved from name/size alone, and no business transaction is replayed to manufacture proof. Detailed per-guarantee evidence and gaps are in the local SOP coverage refresh pending canonical inclusion.

## Next exact gates

1. Guard/Tower exact-main publication and capability checks passed; the PRJ-0007 busy-read defect passed live status checks on four surfaces. Do not replay the transaction.
2. Persist this report through the PRJ-0002 typed document route, then verify committed receipt, finalized status and physical body readback. This document cannot assert its own as-yet unissued receipt. Do not mark full E15 acceptance complete while the standalone-task client gate remains unproven.
3. Continue the separate accepted SOP L0–L10 programme with a fresh canonical rule/source inventory and typed repairs only where identity, eligibility and authorization are proven.

# Governed zone navigation — approved SOP implementation slice

> Execute using subagent-driven development, Luna/high implementation and independent review. Founder authorization relayed 2026-09-25 explicitly includes this missing SOP §10/L5 capability, integration, deployment and governed PRJ-0003 repair. No additional approval round for the same scope.

## Goal and binding design

Maintain one current navigation index per WORKING, REVIEW and DELIVERABLES zone without treating an index as a business document or inventing acceptance. Dropbox remains canonical. Reuse the document request admission, durable recovery, execution journal and provider conditional operations. No new service, dependency, database or production test project.

The existing `00-CURRENT-INDEX.md` is retained when present to preserve links; otherwise use `00-CURRENT.md`. If both exist, reject adoption as a naming conflict rather than delete either. Package `CURRENT.md` is a separate package index, not a duplicate zone index; link it only when proven valid. Identity is `(project_id, zone)`, not the shared basename. Old index bytes and their exact source identity are preserved before replacement.

Public operation: `navigation.reconcile`, document-request identity, zone, expected_project_revision, expected_generation, expected_index (absent or exact basename/object_id/revision_token/content_sha256), created_at. No client-provided rendered content, document inventory, rules, or approval claim. An existing governed index uses its saved identity; untracked index adoption requires the exact observed identity. Fresh admission is mandatory; retries follow frozen server intention, not a fresh meaning under the same request ID.

Canonical sources are managed document heads and exact versions, existing canonical artifact references, and finalized package references. Unknown physical files are not silently called accepted/current; coverage gaps stay explicit. Zone membership follows actual active stage references. Validate path isolation and physical presence before publishing a link. Never reconstruct project business direction from the old index prose.

Navigation receives its own receipt/evidence shape, not `published:true` or a fake document version. Preserve source snapshot, frozen generation input, generated bytes, conditional write result and postcheck evidence in existing canonical execution storage. `committed` remains distinct from physically verified `finalized`.

## Global constraints

- Use existing worktree and branch; preserve untracked worker-configuration.d.ts.
- No direct canonical Dropbox edits, no replay of DOCREQ015 or ambiguous/committed Sujet0.
- PRJ3 remains available for independent operations. No global admission freeze for an index conflict.
- Work in bounded slices with persisted cursor; do not add a full project scan to every document write.
- No secret logging or client-selected bypass. Unsupported evidence fails closed for that index only.
- Do not claim all SOP or V2 gates complete; retain their independent open findings.

## Task 1 — zone identity and resumable navigation engine

Owner: one Luna/high implementer. Files: new `src/documents/zone-navigation.ts`, `src/domain/zone-navigation.ts`, `test/zone-navigation.spec.ts`; use existing persistence and execution modules, do not change public routes yet.

- [ ] RED tests: legacy adoption preserves exact bytes; all three zones have distinct identity; changed source refuses overwrite; dual names conflict; missing target prevents finalization; interruption and same request resume; changed payload same ID refuses.
- [ ] Define typed request and receipt contracts above using strict validation, exporting `NavigationReconcileRequest`, `ZoneNavigationReceipt`, `navigationReconcileSchema`.
- [ ] Implement engine against provider-neutral runtime. Server reads canonical sources. Snapshot cursor and generation intention before effects, CAS source and head, preserve archive first. Use immutable request-scoped records and bounded resumable work; never overwrite changed visible bytes.
- [ ] Expose `reconcile(request, state, admission, budget)` and a resume result distinguishing pending/conflict/finalized. Admission binds exact request/project/zone and server-derived effect scopes. Do not trust public request fields as authority.
- [ ] Run targeted tests and TypeScript; report exact RED/GREEN evidence and interfaces. Independent task review precedes integration.

## Task 2 — governed admission and automatic refresh

Owner: Luna/high after Task1 review. Files: managed-document-request schema, operation-context normalization, ProjectGuard neutral handler/recovery/finalization, Control Tower schema/capability surfaces as required, navigation engine, targeted integration tests.

- [ ] Register navigation.reconcile through the existing typed document ingress and common rule admission. Keep server-owned authorizations and idempotency.
- [ ] Add dedicated receipt/certificate handling; ordinary document finalization must not misinterpret navigation as a published business deliverable.
- [ ] After canonical document/artifact/package changes, persist zone invalidation and wake before declaring required navigation complete. Reuse durable recovery; preserve normal committed receipt even if index refresh is pending.
- [ ] Update adopted inventories incrementally from changed canonical resource references; initial adoption alone inventories existing sources with a cursor. Recovery sees newer verified generation without restoring obsolete content.
- [ ] RED/GREEN integration tests for working.write, review promotion/update, publish, archive, replacement and independent zones; no status-read needed to trigger completion; absent/unadopted legacy indexes do not silently become governed.
- [ ] Expose stale/conflict/pending navigation honestly, without globally blocking unrelated project work. Existing finalized historical receipts remain historical, not a promise of current navigation.

## Task 3 — qualification, deployment and canonical repair

Owner: principal; independent Luna/high review.

- [ ] Review combined diff for admission bypass, concurrency, unbounded work, evidence gaps and naming/link compatibility.
- [ ] Targeted tests, typecheck, existing static gates, full suite and both Worker dry-runs. No unrelated dependency or canonical file changes.
- [ ] Commit reviewed code, PR, exact-SHA CI, merge and deploy exact main using existing workflows. Record Guard and Tower version identities.
- [ ] Refresh PRJ3 state, each legacy index identity, canonical document references and ongoing requests. Coordinate with PRJ3; no artificial business mutation.
- [ ] Submit one exact typed reconciliation per zone, check original request receipt/status after unknown, verify preserved history and resulting bytes/links physically.
- [ ] Verify automatic refresh after a real authorized document change coordinated with PRJ3; do not invent a test decision/document. If no such change is available, record that reception gate as pending rather than claiming it.
- [ ] Notify PRJ3 task and orchestration task with receipt/certificate identities, physical evidence and remaining limits.

## Review focus

1. External edit between snapshot and publication never gets overwritten.
2. Same basename across zones never aliases identity or history.
3. A document receipt committed before index recovery is not lost or replayed.
4. A later generation cannot be overwritten by older delayed recovery.
5. Canonical reference missing physically is visible as a gap, never a valid generated link.

## Execution record

2026-09-25: existing clean code baseline 3d92b49, one unrelated untracked worker-configuration.d.ts. Full baseline suite launched. Read-only Luna/high mapping in progress. V2 standalone-client and broad SOP gates remain open independently.

{
  "name": "project-os",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:persistence-high-risk": "vitest run test/commit-repository.spec.ts test/project-guard-commit-recovery.spec.ts test/registry-guard-recovery.spec.ts test/inbox-isolation.spec.ts test/inbox-replay-cleanup.spec.ts test/materialization-faults.spec.ts test/document-external-edits.spec.ts test/dropbox-document-concurrency.spec.ts test/mutation-gate-faults.spec.ts test/mutation-gate-candidate.spec.ts test/model-lifecycle-concurrency.spec.ts test/project-guard-commit-compat.spec.ts",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "check:persistence-boundary": "node scripts/check-persistence-boundary.mjs",
    "check:mutation-gate-repair-workflow": "node scripts/check-mutation-gate-repair-workflow.mjs",
    "check": "npm run types && npm run typecheck && npm run check:persistence-boundary && npm run check:mutation-gate-repair-workflow && npm test",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "^4.1.0",
    "wrangler": "^4.124.0"
  }
}
